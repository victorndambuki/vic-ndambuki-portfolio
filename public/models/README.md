# 3D Models

Add your .glb files here. Then in projects.js set:
  model: 'your-model.glb'

The viewer auto-fits any size model — no scale adjustments needed.

To export .glb from SolidWorks:
  File → Save As → change type to glTF (.glb)
